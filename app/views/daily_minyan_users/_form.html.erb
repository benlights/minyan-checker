<%= form_with(model: daily_minyan_user, local: true) do |form| %>
  <% if daily_minyan_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(daily_minyan_user.errors.count, "error") %> prohibited this daily_minyan_user from being saved:</h2>

      <ul>
      <% daily_minyan_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
