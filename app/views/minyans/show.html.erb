<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @minyan.name %>
</p>

<p>
  <strong>Time:</strong>
  <%= @minyan.time.strftime("%I:%M %p") %>
</p>

<p>
  <strong>Location:</strong>
  <%= @minyan.location %>
</p>

<p>
  <strong>Notification time:</strong>
  <%= @minyan.notification_time.strftime("%I:%M %p") %>
</p>

<p>
  <strong>Joining Today:</strong>
  <%= @minyan.%>
</p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Contact Method</th>
      <th>Contanct info</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.contact_method %></td>
        <td><%= user.contact_info %></td>
        <td><%= link_to 'Show', minyan_user_path(@minyan, user) %></td>
        <td><%= link_to 'Edit', edit_minyan_user_path(@minyan, user) %></td>
        <td><%= link_to 'Delete User', minyan_user_path(@minyan, user), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New User', new_minyan_user_path(@minyan) %> 
<br>
<br>
<br>
<br>
<%= link_to 'Edit', edit_minyan_path(@minyan) %> |
<%= link_to 'Back', minyans_path %>
