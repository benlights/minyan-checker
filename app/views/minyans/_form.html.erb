<%= form_with(model: minyan, local: true) do |form| %>
  <% if minyan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minyan.errors.count, "error") %> prohibited this minyan from being saved:</h2>

      <ul>
      <% minyan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time, {ampm: true}  %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :notification_time %>
    <%= form.time_select :notification_time, {ampm: true} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
